import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int[] arr = {10, 7, 4, 6, 5, 84, 3, 4};
        // Primo p = new Primo();
        // p.checaPrimo(7);
        // int qtdSomatorio = sc.nextInt();

        // Somatorio
        // int[] numeros = {10, 15, 74, 12, 20};
        // Somatorio calculo = new Somatorio();
        // int resultadoSomatorio = calculo.somatorio(numeros);
        // System.out.println("O resultado do somatório é : " + resultadoSomatorio);

        // Quicksort
        // Quicksort.ordernar(arr, 4, arr.length - 1);
        // System.out.println("Ordenação " + Arrays.toString(arr));

        // Fibonacci
        /*
        System.out.print("Digite a posição desejada na sequência de Fibonacci: ");
        int posicao = sc.nextInt();

        int fib = Fibonacci.calcular(posicao);

        System.out.println("O valor da posição " + posicao + " na sequência de Fibonacci é: " + fib);

        sc.close();

         */
    }
}
